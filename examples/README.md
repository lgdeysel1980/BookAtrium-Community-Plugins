# Example registry manifests

**EXAMPLE ONLY — NOT INCLUDED IN THE LIVE CATALOGUE.**

These files illustrate registry entry shape. The live catalogue is built only from `plugins/*.json`.
Do not copy these into `plugins/` unless a real public immutable GitHub Release exists with matching SHA-256.
